import React from 'react';
import {Body} from "../components";
import CreateModal from "../modals/CreateModal";

const Doc = () => {
    return (
        <div>
            <CreateModal/>
        </div>
    );
};

export default Doc;